export const initialState = () => {
  return {
    shoppingList: [
      {
        id: '1',
        body: 'first post',
      },
    ],
  };
};

export const reducer = (state, actions) => {
  return state;
};
